'''
Created on 1 abr. 2019
Ejercicio libro 273. Función que devuelve el valor absoluto de la máxima diferencia entre dos números consecutivos de una lista.
@author: joseluis
'''


def max_diferencia_consecutiv_lista (lista):
    diferencia = 0
    max_diferencia = 0
    mayor = 0
    for i in range (len(lista)-1):
        diferencia = ((lista[i+1]) - (lista[i]))
        mayor = diferencia
        if max_diferencia > mayor:
            mayor = max_diferencia
        
        
    return max_diferencia
 

lista1 = [3, 7, 14, -20, 56, 4, 7, 8, 9] 

print(max_diferencia_consecutiv_lista(lista1))

    
